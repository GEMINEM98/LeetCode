
void swap(char* a,char* b)
{
    char c=*a;
    *a=*b;
    *b=c;
}

void reverseString(char* s, int sSize)
{
    int begin=0;
    int end=sSize-1;
    while(begin<end)
    {
        swap(&s[begin],&s[end]);
        ++begin;
        --end;
    }
    return 0;
}
